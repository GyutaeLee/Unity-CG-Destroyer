using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class TScene1Manager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // RenderPipelineAsset hdrp = Resources.Load<RenderPipelineAsset>("_CHAN/HDRPAsset/TScene1HDRP.assset");
        // GraphicsSettings.renderPipelineAsset = hdrp;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
